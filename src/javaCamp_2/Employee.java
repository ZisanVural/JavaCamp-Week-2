package javaCamp_2;

public class Employee extends Person { // Employee class inherits from the Person class

	double salary; // We didn't put "public" at the beginning because "public" is the default in java
}
